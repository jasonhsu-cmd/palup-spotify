packages:
  - "packages/*"
allowBuilds:
  # Not run offline: the SDK is lazily imported and only executes when GCP creds are present.
  # Approve these when wiring the live Vertex path (vet-then-adopt, build-automation.md §4).
  '@google/genai': false
  esbuild: true
  protobufjs: false
