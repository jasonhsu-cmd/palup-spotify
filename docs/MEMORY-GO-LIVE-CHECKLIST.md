# Cross-visit memory — go-live checklist

> **Status: NOT READY. Several conditions below are unmet.** This document is the single gate list for
> flipping `MEMORY_ADR_ACCEPTED` (`packages/widget-memory/src/flag.ts`) and moving ADR-0015 from
> *Proposed* to *Accepted*. That flip is **human-only** (CLAUDE.md §3, ADR-0015 Status): it requires a
> named human owner, a recorded `security-reviewer` sign-off, and a recorded **legal** sign-off. No build
> agent may perform it, and no amount of green CI substitutes for the legal conditions.

**What the flip actually does.** `isMemoryEnabled()` is `MEMORY_ENABLED === "true" && MEMORY_ADR_ACCEPTED`.
Both must be true. The env var alone can never enable memory (NN#1) — the const is a build-time change that
goes through code review. Until then the entire subsystem is inert: no fact is written, none is recalled,
and production behaviour is byte-identical to before the program.

**How to read this list.** Every item is either **MET** (with the evidence), **OPEN** (with what is
missing), or **ACCEPT** (a residual risk that must be consciously signed off rather than fixed). An item
being "in a PR" is *not* met — merged and verified is met.

_Last updated: 2026-08-04. Update the status column in the same PR that closes an item._

_PRs #144 (B9), #145 (B1), #146 (A1/A2 drafts), #150 (B2), #151 (B3/B4) have all landed on `main` — see
those rows for evidence. #146 landed DRAFTS only; A1/A2 stay OPEN until counsel executes them. #152
(subject-scoped memory auth) is open, addressing C1/C2/B12's consent-resolution hazard (see those rows), and
carries the B11 manage-panel honesty fix._

_**Named-owner decision, 2026-08-04 — the guest→account link is WON'T DO.** Signed-out visitors are treated
as anonymous guests, governed by the guest subject's own recorded choice. Consequences, all recorded in the
rows: **C14 is ACCEPTED by design** (it is a write-when-denied, and accepting it does not make it
privacy-conservative); **B12 splits** into (a) the link, WON'T DO, and (b) fact stranding, still OPEN; **B4**
and **C6** lose "B12's migration" as a candidate remedy, leaving a scheduled job. Branches
`feat/b12-guest-account-link` and `feat/c15-revocable-link` were pushed for review and **abandoned — neither
merged, and no PR was opened for either**. The root cause behind all of it is **C1**: nothing proves a
client-supplied `anonId` belongs to its caller._

_**Named-owner decision, 2026-08-04 — C1 is ACCEPTED AS IS.** Obtaining an `anonId` realistically requires
access to the victim's browser, at which point little downstream is preventable regardless, so hardening the
id was judged not worth its cost. This decision is load-bearing for the §C set: **C8, C10 and C14 stand
because C1 does.** Restricting memory to signed-in shoppers would have dissolved all four; it was considered
and not taken, because it costs the guest-memory feature entirely._

---

## A. Legal and governance — all OPEN, all human

These cannot be closed by engineering work. They are the reason this checklist exists.

| # | Condition | Status |
|---|---|---|
| A1 | **Privacy notice covering cross-visit memory, executed.** Draft exists (`docs/legal/memory-privacy-notice-draft.md`) — explicitly *not* legal advice, *not* executed. | **OPEN** |
| A2 | **DPA / processor terms covering health (Art-9) data, executed.** Draft exists (`docs/legal/memory-dpa-addendum-draft.md`). `docs/legal/provisions-brief.md` §0 records that no agreements currently exist. | **OPEN** |
| A3 | **Legal sign-off recorded in ADR-0015**, resolving `docs/legal/memory-open-questions-for-counsel.md` — in particular the US Consent-2 fail-closed default, the sliding-renewal retention model, and the Art-9 lawful basis. | **OPEN** |
| A4 | **`security-reviewer` sign-off recorded at the flip.** Per-PR security reviews across the program are *not* this: ADR-0015's Status line requires a sign-off on enablement itself. | **OPEN** |
| A5 | **Legal review of the shopper-facing consent copy** (the health/Consent-2 prompt shipped in PR-11b/11c is health-consent language). | **OPEN** |
| A6 | **DPIA / lawful-basis coverage for the classifier running on shopper messages.** Once live, `classifyFact` inspects every message to decide whether to *ask* for consent (pure, in-memory, stores nothing — but it does process message text). | **OPEN** |
| A7 | **ADR-0015 Status → Accepted** and **named human owner merges the flip.** | **OPEN** |

## B. Technical preconditions

| # | Condition | Status |
|---|---|---|
| B1 | **Durable vector storage.** Production ran `createInMemoryVectorStore()` — memory would evaporate on restart, not be shared across instances, and `/forget` erasure would not be real (Inv 5). `PostgresVectorStore` (state-postgres) now backs it when `DATABASE_URL` is set. | **MET — PR #145** |
| B2 | **Encryption-at-rest for special-category facts** (Inv 9). Art-9 payloads were stored in plaintext. Also closed two holes found in review: `disposition.value` persisted in the clear, and an Art-9 `sourceQuote` riding an ordinary fact escaped the health gate (no Consent 2, no encryption, invisible to Consent-2 withdrawal). | **MET — PR #150** |
| B3 | **Memory-live implies enforced widget auth.** `WIDGET_AUTH_REQUIRED` defaults false; in that window `/consent` and the destructive `/forget` are callable unauthenticated. `assertMemoryAuthCoupling` (server.ts) now fails boot fast rather than flipping that default. | **MET — PR #151** |
| B4 | **Retention actually enforced** (Inv 4, "expiry is enforced, not aspirational"). `sweepExpired` had no production caller; an opportunistic per-subject sweep was added on `/chat` (audit-before-delete, so a destructive action is never invisible). **Round 3 (N1) reopened this row.** Subject-scoped auth made the memory subject SWITCH at sign-in, and the sweep visits only the served subject — so a signed-in shopper's guest-era namespace is never swept ON A SIGNED-IN TURN (on a signed-out turn `memorySubject` IS the guest anonId, so it is swept then — the gap is specific to signed-in traffic, which errs toward more reclamation than an earlier draft of this row claimed) and its facts outlive their TTL indefinitely. **This PR CREATED that class**: before `memorySubjectId`, a signed-in shopper's subject WAS the `anonId`, so no second unvisited namespace existed. It is not pre-existing. Widening the sweep to a client-supplied `anonId` was attempted and deliberately REVERTED — the refusal is upheld by both reviewers, but the ORIGINAL rationale recorded here was wrong and is corrected: a sweep deletes only already-EXPIRED records (a strict subset of what the unauthenticated `/forget` path already permits with no token), returns nothing to the caller, and never reaches the model — so it does NOT violate the property F1 protects (no cross-subject fact text reaching the prompt/reply). The real reasons to refuse are (i) ATTRIBUTABILITY — an `erase.subject` on a user-initiated `/forget` is legible to an operator, an implicit per-turn `ttl_sweep` against a namespace the caller merely NAMED is not — and (ii) proactive reclamation belongs in a scheduled/admin job keyed off SERVER-known subjects, as `retention.ts`'s own header already says. The correct fix is server-side and, since the guest→account link is WON'T DO (B12(a), 2026-08-04), a scheduled job was the only remaining candidate. **BUILT 2026-08-05.** `sweepAllSubjects` (`widget-memory/src/retention.ts`) enumerates a per-tenant **subject index** (`widget-memory/src/subject-index.ts`) and sweeps the subjects the serving path structurally cannot reach, driven by a scheduled job (`widget-backend/src/jobs/retention-sweep.ts`, `pnpm sweep`). **Why an index was needed at all:** `VectorPort` has no namespace-listing operation and adding one would impose a non-portable requirement on every adapter (ADR-0001), while the `memory_consent` KV — the only existing per-subject index — holds no row for the common US case of a shopper who never answered the prompt and writes on `"unknown"`. So the index is driven by the WRITE: `remember()` records a subject whenever facts actually land, best-effort (an index-write failure never fails the turn or loses a fact; the honest cost is that such a subject is reclaimed only on their own return, i.e. pre-B4 behaviour). Entries are retired when a sweep confirms the namespace is empty, so the index cannot grow without bound. **Why a JOB and not the admin HTTP endpoint an earlier revision of this row anticipated:** widget-backend has no admin authentication of any kind, so an endpoint would have meant a new internet-reachable route whose entire purpose is mass deletion, plus a new shared secret to guard it. A scheduled process needs neither. **Safety, each mutation-checked or test-locked:** the kill switch is honoured per tenant BEFORE any deletion (removing that check makes exactly two tests red — verified); expiry remains the only predicate, so live facts and consent-withdrawn-but-unexpired facts are untouched; one tenant's or one subject's failure never aborts the run; runs are bounded by `maxSubjects` and REPORT what they left behind rather than a truncated pass reading as complete; every delete keeps `sweepExpired`'s audit-before-delete ordering. **Privacy note:** the index stores raw subject ids + a timestamp — no fact text — which is not a new data class, since `memory_consent` already stores the same ids in the same KV. **Still not covered by this row:** guest-era FACT CONTINUITY (B12(b)) is a separate, still-open gap — a signed-in shopper's guest-era facts are now RECLAIMED on schedule, but they are still not readable as part of their account. | **JOB PROVEN, SCHEDULE STILL MISSING — 2026-08-06.** The Cloud Run Job `palup-retention-sweep` exists and ran successfully against staging's real Cloud SQL: `[sweep] store=postgres vector=postgres tenants=1`, `tenant=demo visited=0 deleted=0 retired=0 failed=0 remaining=0`, `exit(0)`. `store=postgres` is the load-bearing line — it proves the `/cloudsql` unix socket actually opened. Config verified: `command=[pnpm] args=[sweep]`, `cloudsql=palup-jason:us-central1:palup-staging`, `DATABASE_URL` from `palup-staging-database-url`, maxRetries 3, timeout 600s. **TWO GAPS KEEP THIS OPEN.** (1) **Nothing schedules it** — that run was manual, so retention is still unenforced for a shopper who never returns. (2) The job carries no `AUDIT_HMAC_SECRET`, so its audit `subjectRef`s would be unkeyed sha256 while serving's are now keyed HMAC (B5) — the SAME subject would get TWO different refs, and since `rs_audit` is `prev_hash`-chained and INSERT-only, a wrong ref cannot be corrected afterwards. B5's secret + grant now exist, so this is a one-command `gcloud run jobs update`. **Fix it before the job ever does real work, not after.** Also still unproven: `visited=0` means no `ttl_sweep` audit was written (by design — `retention.ts` audits only a subject that had something decided for deletion), so nothing yet confirms those entries reach the sink, nor that the audit-before-delete ordering and the 'audit fails ⇒ skip the delete' guard hold against the real store. |
| B5 | **Prod audit sink is the immutable, hash-chained log** — not `InMemoryRuntimeStore`. `consent.record`, `write.*`, `recall`, `ttl_renew`, `ttl_sweep`, and `erase.subject` must all be durably recorded. Verify against the deployed configuration, not the default. | **MET — 2026-08-06, verified in the LIVE deployment.** The durable sink was already correct (`/health` reports `store: postgres, vector: postgres`; the chain is an unkeyed `sha256(canonicalize(base))`, `platform-ports/src/audit-hash.ts`, so it never needed a key). What was missing was the keyed `subjectRef`, and it took THREE steps, not two: create the secret, **grant the RUNTIME service account `roles/secretmanager.secretAccessor` on it**, then set `AUDIT_HMAC_SECRET_NAME`. The middle step was initially skipped and broke the deploy pipeline — see `DEPLOY.md` for why `roles/editor` is not sufficient (it carries 18 `secretmanager.*` permissions but deliberately NOT `secretmanager.versions.access`, verified against `gcloud iam roles describe roles/editor`) and why the grant belongs on the runtime SA rather than the deploy SA (the deployer writes a POINTER; the container performs the READ). Now verified live: `AUDIT_HMAC_SECRET` is mounted from secret `audit-hmac-secret` on revision `00162-phk` (created == ready, no stuck revision), and the full serving path returns 200 on `/health`, `/widget/token` and an authenticated `/chat` with a real grounded reply. So `subjectRef` is now a keyed HMAC and the identity audit is no longer skipped. **Do not rotate this key casually:** changing it changes every `subjectRef`, so audit rows for the same subject stop correlating across the change. |
| B6 | **Erasure completeness re-confirmed against the real adapter.** Inv 5 is adapter-dependent; only the in-memory oracle and pglite are proven today. Re-run the erasure proof against the actual cloud store. | **MET FOR THE ADAPTER — 2026-08-06.** Re-run against a REAL Postgres 16 SERVER (not pglite) through the production constructor `pgPoolSqlFromUrl` — i.e. the `pg` driver, wire protocol and connection pool that pglite bypasses by running in-process, which was the actual gap this row named. The proof asserts PHYSICAL deletion by querying `vp_records` directly, so a soft-delete or filtered read path cannot pass it, and it is **mutation-verified**: rewriting `deleteNamespace` to `... AND false` turns it red with 'rows survive in vp_records', reverting turns it green. It also pins scope (a bystander subject in the same tenant is untouched), cross-instance durability, and the `erase.subject` audit row. `packages/widget-backend/test/b6-erasure-real-postgres.test.ts`, skipped unless `B6_DATABASE_URL` is set so CI needs no database service. Separately, the LIVE path was exercised against the actual Cloud SQL instance: `POST /forget` on staging returned HTTP 200 `{ok:true}` with no server error. **Residual, stated rather than glossed:** that live call erased *nothing*, because memory is off and no fact can exist to delete — the vector-factory's own comment warns that `{ok:true}` over an empty namespace is indistinguishable from a successful erase. Erasure of REAL data in Cloud SQL is therefore only observable once memory is on, which is the canary's job (§D step 8), not this row's. |
| B7 | **Consent write/read bar reconciled — DECIDED AND BUILT (named-owner decision, 2026-08-05: "unknown and yes work the same in non-GDPR region").** `decideMemoryWrite` allowed *ordinary* US writes on `"unknown"` (opt-out regime) while recall required a literal `"in"` (`brain.ts` filtered every recalled fact through `consentedAtReadTime` before any reached the prompt), so the system accumulated ordinary US facts it could never surface. **The READ bar moved down to meet the write bar**, not the reverse. Both gates — plus the sliding-retention renewal — now call ONE rule, `consentPermits` (`widget-brain/src/consent-rules.ts`), which lives in that package solely to respect the documented no-dep-cycle contract (`widget-memory` → `widget-brain`, never the reverse). **Scope, each half regression-locked:** ordinary tier only; `region === "us"` only (eu/uk/other/absent unchanged and still need explicit `"in"`); **special-category untouched in EVERY region including the US** — Art-9 memory still requires explicit `memorySpecial === "in"` per the ADR-0015 amendment ratified by legal, which this did not reopen; WRITE behaviour unchanged everywhere. **User-visible consequence, stated plainly:** US shoppers who never answered the prompt will now receive replies personalised from remembered facts, where previously the facts were stored but never used. **This also fixed a live bug it exposed:** `server.ts`'s recall wrapper hardcoded `consent1/consent2: "unknown"`, making the Inv-4 sliding-TTL renewal structurally unreachable on `/chat` for every shopper regardless of consent — previously documented as a deferred gap. It survived because every `ttl_renew` test lived inside `widget-memory`, where that wrapper does not exist; `chat-retention-renewal.test.ts` now covers the wiring at the server and was mutation-checked against the original code. | **MET — decided + built 2026-08-05** |
| B8 | **Encryption key provisioned** (`MEMORY_ENCRYPTION_KEY`, per tenant) in the secrets port. Without it, special-category writes are refused (fail-closed by design) — consented health facts are not stored, though a `write.refused` audit now records it. Rotation is two-step (`_previous`) — see the runbook in `DEPLOY.md`; a one-step rotation is **irrecoverable**. | **MET — 2026-08-06, verified against the deployed secret.** `MEMORY_ENCRYPTION_KEY` is present for tenant `demo` in the `palup-secrets` payload at **44 bytes** of UTF-8 material, comfortably over the 16-byte `MIN_KEY_MATERIAL_BYTES` floor (`platform-ports/src/crypto-port.ts`), so `deriveKey` will not refuse it. Verified by parsing the secret and printing key NAMES and byte LENGTHS only — the material itself was never printed. Memory facts use the DEFAULT key scope, so the bare name is the one that matters; the separate `MEMORY_ENCRYPTION_KEY__merchant-cred` scope is unrelated and still unset (it is only needed at Shopify install time). **Rotation remains the trap:** one-step replacement is irrecoverable — see `DEPLOY.md`. |
| B9 | **FAIR-1 fairness floor is non-vacuous.** The graders measured `personaPriceInvariance` on a brain that could not see persona signals, so every candidate scored a perfect 1.0. Fixed + regression-locked. | **MET — PR #144** |
| B10 | **Kill switch halts memory.** A kill-switched turn must not write memory. | **MET — #139** |
| B11 | **Shopper controls shipped**: consent capture, manage panel, and "forget everything" (erasure + anonId reset). **Panel honesty fix (PR #152):** the manage panel bound its toggle to `memState.consent1 === "in"` read from localStorage, while the server's ordinary rule in the default US region is the opt-out regime `!== "out"` — so the tri-state `"unknown"` meant memory was **ON while the box rendered unchecked**, for every US shopper who never answered the prompt. Pre-existing on `main`, unrelated to subject-scoped auth, and it also supplied the misleading half of C14 and C7. `/chat` and `/consent` now both return `memoryActive` — the effective write capability for the subject actually served — derived from the same single input object `remember()`'s own consent gate consumes, so the panel and the write path cannot disagree. Verified by execution: `manage-panel-honesty.test.ts` asserts the reported field against the real upsert count on the same turn, and the e2e case was mutation-checked (reverting only the panel binding fails exactly that test). | **MET — #142, #143; panel honesty #152** |
| B12 | **Guest → account memory continuity.** `mergeGuestIntoAccount` exists but has **no production caller**, and subject-scoped auth makes the subject *switch* at sign-in (`anonId` → `acct:<id>`). A shopper who used memory as a guest and then signs in starts a fresh subject: their guest FACTS are stranded under the old namespace — that data-migration gap is still unbuilt. The CONSENT half of this row previously claimed the un-migrated guest record "does not resolve (fail-closed `unknown` — safe)"; that was **INVERTED, not safe, in the default US region**: `decideMemoryWrite`'s opt-out regime reads an unresolved `"unknown"` as ALLOWED, so a guest-recorded explicit **opt-out** silently stopped applying the instant the shopper signed in — proven by execution by two independent security reviews. PR #152 closes that specific consent-resolution hazard with a restrictive merge (`mergeAccountConsent`, widget-memory/src/consent.ts: a guest `"out"` always wins over an unresolved/`"unknown"` account record; a guest `"in"` is never adopted for the account). **Round-3 correction (N2):** the previous version of this row said the merge "persists nothing" and that "persisting it is the only thing that makes an opt-out durable" — that is now WRONG in one direction: round 3 (N2) made the merge's RESTRICTIVE ("out") outcome durable — whenever a signed-in shopper's turn merges a guest `"out"` into a differing account record, that outcome is now written back to the account's own consent record (`recordConsent`), so a LATER turn presenting no guest `anonId` at all (new device, cleared storage, post-`forgetMe()`) still correctly stays opted out (see `consent-restrictive-merge.test.ts`'s "N2 — ... becomes DURABLE"). A guest `"in"` is still never durably adopted (unchanged; would let anyone borrow a stranger's opt-in). This closes the DURABILITY half of the consent-resolution hazard, not the FACT-migration this row is fundamentally about — `mergeGuestIntoAccount` still has no production caller, so a guest-era shopper's actual remembered FACTS remain stranded under the old namespace regardless. Also corrected: this was never "harmless while inert" in the sense of being gated off — `/consent` and `/forget` are **not** behind the `memoryServiceEnabled` double gate (flag.ts); whatever inertness exists today is enforced by the CLIENT not calling them while memory is off, not by the gate itself. **Must be wired before go-live**, migrating the facts into the account namespace (the consent-durability half N2 covers is a genuine improvement, not a substitute for the fact migration). **This row has TWO halves and they now have DIFFERENT verdicts — do not collapse them into one.** (a) **The guest→account LINK: WON'T DO** (named-owner decision, 2026-08-04). It was built on `feat/b12-guest-account-link` and made revocable on `feat/c15-revocable-link`; **neither merged**, both abandoned. A link keyed on a client-supplied `anonId` is a squatting target when immutable (third-party denial with no non-destructive escape) and a permissive-direction capability when revocable (proven by execution at `d654c66`: re-pointing a victim's link defeats their recorded opt-out on signed-out turns). Signed-out visitors are treated as anonymous guests instead — see C14, which this row no longer remedies. A fact MIGRATION was also built on top of the link and **removed as a data-theft vector** before any merge (an attacker holding a victim's `anonId` could pull the victim's facts into their own namespace); it is not in any shipped code. (b) **The FACT-STRANDING half stays OPEN and is unaffected by that decision** — `mergeGuestIntoAccount` still has no production caller, so a shopper's guest-era facts remain stranded under the old namespace after sign-in. Any future remedy for (b) must not reintroduce (a): the migration has to be driven by a server-known association, which is exactly what does not exist today (C1). | **OPEN — fact stranding still not built; the guest→account LINK is WON'T DO (2026-08-04), so this row is no longer C14's remedy** (consent-resolution hazard mitigated by PR #152 round 2; consent DURABILITY additionally closed by PR #152 round 3/N2 — see C7 for a new, accepted trade-off this introduces) |

## C. Residual risks to ACCEPT explicitly

These are known, bounded, and not planned to be fixed before go-live. The named owner must accept them in
writing — silence is not acceptance.

| # | Residual | Why it is bounded |
|---|---|---|
| C1 | **`anonId` is a bearer capability — for ANONYMOUS GUESTS ONLY (narrowed), and only for data written AFTER sign-in with the principal actually sent.** Read literally, not as a general closure: **closed for data written after sign-in, once the client sends the principal** — `x-shopper-token`, now sent on `/chat`, `/consent`, AND `/forget` alike (PR #152 fixed the latter two, which the shipped widget previously omitted it from). Two things this does **not** close: (i) neither `/consent` nor `/forget` actually *requires* the shopper token — an attacker simply omits it and the request falls back to the unauthenticated `anonId` path exactly as before, for **any** caller, well-formed guest id in hand; (ii) guest-era facts are never migrated into the account (B12(b), unbuilt), so a shopper with guest history stays fully bearer-exposed on that guest-era data even after signing in. **This row is the unfixed root cause behind C14's acceptance, C8, C10, and the withdrawal of B12(a)** — every remedy attempted for those needed a way to tell whether a client-supplied `anonId` belongs to its caller, and there is none. **DECIDED — ACCEPTED AS IS (named-owner decision, 2026-08-04).** The owner's rationale, recorded because it is what makes the acceptance reasonable rather than merely convenient: **the realistic way to obtain someone's `anonId` is access to their browser, and once an attacker has that, very little downstream can be prevented anyway** — so hardening the id (server-issued, signed, HttpOnly) buys less than it appears to, and would not help at all in the shared-browser case that dominates the threat. Two alternatives were considered and NOT taken: binding the id to a server-issued credential (rejected as above), and restricting memory to signed-in shoppers only — which would have dissolved this row along with C8, C10 and C14, at the cost of the entire guest-memory feature. **The consequence to sign knowingly:** accepting this row is what keeps C8, C10, and C14 standing, because each descends from it; a signer accepting C1 is accepting that set, not one row. | 128-bit CSPRNG (not enumerable), per-tenant, rate-limited, kill-guarded. Blast radius is the victim's own preferences; this **does** include read access — `/chat` recall queries the victim's namespace and can surface their fact text into the model's reply for whoever holds their `anonId` (pre-existing behavior, not introduced or changed by this PR — the prior "no read access" claim here was false before this PR too). No cross-tenant reach; orders/account untouched. |
| C2 | **Subject-scoped auth — closed end-to-end for a verified shopper's OWN `/chat`, `/consent`, and `/forget` calls, as of PR #152.** Widget auth binds the *tenant*; the shopper subject is additionally bound to the verified `x-shopper-token` principal (`memorySubjectId`, `identity.ts`), gated on the explicit `verified` flag and the cross-shop tenant check, on all three surfaces — and the shipped widget now sends that header on all three (previously `/consent`/`/forget` did not: a signed-in shopper's own calls looked anonymous to the server, so `/forget` returned `{ok:true}` while erasing nothing — PR #152 BLOCK-2). A guest-recorded **opt-out** is honored at sign-in by a restrictive merge (`mergeAccountConsent`) and — since round 3 (N2) — **written through to the account's durable consent record** (`recordConsent`), so it survives a fresh browser, cleared storage, and the widget's own `forgetMe()`. **Round 6-7 (BLOCK-A → residual C14):** that claim holds only for VERIFIED turns. The merge is one-directional, so an authenticated opt-out does NOT govern the same browser's signed-OUT turns. THREE attempts to fix it in `/consent` were each rejected in review (a toggle trap, a silent fail-open with no repair path, and finally a version that broke this row's own "a supplied anonId is IGNORED" property). It is now DISCLOSED as C14 rather than half-fixed, and as of the 2026-08-04 owner decision it is ACCEPTED there by design rather than pending a remedy — an earlier revision of this row said "the real fix is B12", which is no longer true (B12(a) is WON'T DO). Only the restrictive direction is persisted: a guest `"in"` is NEVER adopted for an account. (An earlier revision of this row said the merge was read-time only and wrote nothing — that was true of round 2 and became FALSE in round 3; see C7/C8/C10 for what the durable write itself costs.) **PRECONDITION (security review round 5): all of this is gated on `SHOPPER_AUTH=true`** — `SHOPPER_AUTH_ENABLED = SHOPPER_AUTH === "true" && WIDGET_AUTH_REQUIRED` (`server.ts`). With `SHOPPER_AUTH` unset, the verified-shopper derivation yields `undefined` on all three surfaces (`/consent` and `/forget` via `verifiedShopperIdFor`; `/chat` via its own inline derivation gated on the same flag — see C13), every shopper falls back to the C1 bearer-`anonId` path, and the write-through never fires — this row would then be VACUOUSLY true rather than false. It is in §D step 6 for that reason. Guests remain principal-less by construction. | Closed for an authenticated shopper's own calls. Residuals: C1 (guest bearer capability), C6/B4 (guest-era facts un-erasable by sweep, and B12's un-migrated facts), C7/C8/C10 (consequences of the durable write-through). |
| C14 | **An authenticated opt-out does not govern that same browser's signed-OUT turns — ACCEPTED BY DESIGN (named-owner decision, 2026-08-04).** On an unverified turn the subject is the guest `anonId`, the `acct:` row is never consulted, and the US opt-out regime reads an unresolved `"unknown"` as ALLOWED — so a shopper who opts out while signed in still has facts written once their token expires (sessionStorage, 1h default TTL: a new tab suffices). **This PR created the class** — pre-PR the signed-in write landed on the guest row itself, so it governed those turns. **The decision:** a signed-out visitor is treated as an anonymous guest and governed by the GUEST subject's own recorded choice; no server-side guest→account link is recorded. Pinned by tests so the behaviour cannot change silently (`consent-restrictive-merge.test.ts`, and the C14 case in `manage-panel-honesty.test.ts` which asserts it against the actual upsert count). | Direction is a WRITE-when-denied, so unlike the other residuals this one is **not** privacy-conservative — accepting it does not make it so, and it should still weigh heaviest in sign-off. **Why accepted rather than fixed:** governing a signed-out browser by an account's choice requires a server-recorded association keyed on a client-supplied `anonId`, and nothing in this system proves an `anonId` belongs to its caller (C1 — the unfixed root cause). Three in-`/consent` fixes were attempted and rejected in review. A server-side link was then built (branch `feat/b12-guest-account-link`) and a revocable version of it after that (`feat/c15-revocable-link`); **both were withdrawn, neither merged.** The link made the `anonId` a squatting target — a third party binding a victim's `anonId` to their own account could deny that victim indefinitely, with no non-destructive escape. Making it revocable (last-verified-writer-wins) was then proven by execution on `feat/c15-revocable-link@d654c66` to hand an attacker a **permissive-direction** capability the first-writer-wins version denied: re-pointing a victim's link at a non-opted-out account stops the victim's recorded `out`/`out` from governing their signed-out turns (same probe: 0 facts written on the b12 base, 1 on the c15 branch). Every version of the remedy traded this row for a worse one. **What DID change:** the shopper is no longer misled about it — an earlier revision of this row noted the manage panel renders "off" during exactly this window; the panel now renders the server's effective state for the subject actually being served (see B11), so the residual is visible to the shopper rather than contradicted by the UI. |
| C3 | **`query` is a brute-force namespace scan, not ANN.** | Embeddings were dropped in scope (FAST-V1); the only consumer uses the empty-text list-all idiom. Documented in-adapter. |
| C4 | **Free-text willingness-to-pay is prompt-governed, not structural.** The *disposition* WTP path is structurally blocked (no `inferred` provenance); a WTP claim inside free-text fact content is caught by the distiller prompt + eval, not a classifier. | Contained today by the read-time consent gate; confirm the distiller eval covers it. |
| C5 | **Merchant per-store disable toggle and the Invariant-11 sensitivity-policy authorship/review process** are ADR-0015 open items, not built. | Tenant policy may only *narrow* what is remembered; absence means the conservative default applies. |
| C6 | **RESOLVED — PR #152 round 3 (N1).** Previously: a signed-in shopper's "forget everything" did NOT erase their guest-era facts, while the UI reported success — `/forget` targeted `acct:<shopperId>` only and ignored any supplied `anonId`. **Fix landed:** `/forget` now ALSO erases a validated `anonId` presented alongside the shopper token (the shipped widget's own `forgetMe()` already sends both — `index.html`'s `prevAnonId`), so both namespaces end up empty and the UI's "Done — I've cleared what I remembered" is true whenever the client presents both. Proven by execution: `forget.test.ts`'s new N1 test (red without the fix, green with it). This is safe by the SAME reasoning C1 already accepts: the guest path a few lines above already lets an *unauthenticated* caller erase any `anonId` they hold, so doing the same erase on a verified turn grants an attacker nothing new — see the (intentionally reversed) "THE ATTACK: POST /forget" test in `subject-scoped-memory-auth.test.ts`. **Residual, moved to B4:** the erasure is only as good as the client presenting that `anonId` — it is on-demand (via `/forget`), not proactive; the *opportunistic per-turn TTL sweep* on `/chat` still does NOT visit a signed-in shopper's guest-era namespace (attempting that regressed the protected F1 recall test — see B4). A shopper who never calls "forget everything" and whose guest-era facts simply expire now HAS them physically reclaimed by the scheduled sweep job (B4, built 2026-08-05) once it is scheduled in the target environment — an earlier revision of this row said "until a scheduled sweep job exists", which is no longer the outstanding condition; the outstanding condition is now a deploy step (§D). B12's migration was never the remedy here: B12(a) is WON'T DO as of 2026-08-04 and B12(b) remains unbuilt, but neither is required for reclamation. | N/A — resolved; see B4 for the remaining opportunistic-sweep residual.  **Caveat (round 3):** `/forget` erases the FACT namespaces only — `eraseSubject` never touches the `memory_consent` KV, so both the guest row and the autonomously-created `acct:` row SURVIVE erasure (see C11). The UI's "Done — I've cleared what I remembered" is true of remembered facts, not of the consent record. |
| C7 | **A stale guest opt-out defeats that same shopper's later, authenticated opt-IN.** Guest records `"out"`, shopper signs in and explicitly grants consent against `acct:<id>` — the read-time merge still resolves `"out"`, so no writes occur. (An earlier revision of this row added "while the manage panel (local state) shows memory as on" — **no longer true**: the panel now renders the server's effective state for the subject being served, so it shows this denial rather than contradicting it. See B11.) **Round-3 update (N2) — restated, NOT fixed, made STICKIER by design:** N2 durabilizes the merge's restrictive output (see B12), and "out always wins outright" (`mergeConsentTier`) is what makes a guest opt-out survive sign-in at all (the whole point of N2). The same rule now means a stale guest `"out"` can durably OVERWRITE the account's fresh, explicit `"in"` the next time the client presents that guest `anonId` — proven by `consent-restrictive-merge.test.ts`'s new "C7 (restated)" test: after the shopper's explicit `/consent` opt back IN, a turn re-presenting the stale guest `anonId` re-durabilizes `"out"`, and — unlike pre-N2 — a SUBSEQUENT turn presenting NO guest `anonId` at all now ALSO stays denied (previously, omitting the id would have let the account's fresh `"in"` govern again). This is the accepted cost of N2's write-through, not an oversight; N2 was scoped (per its own requirement) to write through ONLY the restrictive direction. | Still fails in the privacy-safe direction (never silently re-permits a write); the residual has shifted from "per-turn, easily self-resolved by omitting the anonId" to "durable until the stale guest record itself is cleared." **Correction (round 5, PROVEN BY EXECUTION — `consent-restrictive-merge.test.ts` "BLOCK-1(a)/(b)"):** the escape is real, ORDER-SENSITIVE, and NOT necessarily lossy. (1) FIRST neutralise the guest subject — either record the desired value on it from a SIGNED-OUT client (no shopper token; `/consent` then binds to the guest subject — nothing is erased), or drop the guest `anonId` via forget-me (which DOES erase facts); THEN (2) record it on the account subject while signed in. Doing (2) first is undone by the next turn presenting the stale id. An earlier revision of this row claimed erasure was the only route and that the residual was unbounded in time — both wrong. What remains genuinely unowned is retiring the guest consent ROW itself. |
| C8 | **NEW (PR #152 round 3, N3) — cross-subject consent oracle / denial primitive.** `/chat`'s restrictive-merge lookup (`server.ts`, feeding `mergeAccountConsent`) does a `lookupConsent` against a **caller-supplied** `anonId` (any signed-in shopper's own turn, as long as they attach a well-formed `anonId`) and lets that stranger's row govern the caller's OWN write this turn. A caller who wants to learn a target's `memoryOrdinary` tier can attach the target's `anonId` and observe whether their own write went through: refused ⇒ the target's row says `"out"`; succeeded ⇒ it does not. This is a 1-bit read of another subject's consent state plus a memory-denial primitive, newly reachable from an AUTHENTICATED turn rather than the anonymous C1 bearer-capability path. | Blast radius is bounded — holding a 128-bit `anonId` already grants the larger C1 read (recall queries + serves the target's own fact text), so this adds a narrower, indirect 1-bit signal on top of an already-larger exposure, not a new class of access; the standard per-IP/per-tenant rate limits apply. **An earlier draft additionally claimed N2's write-through narrows this to "one usable probe per attacker account" — that is FALSE and has been struck** (proven by execution in security review: probes against targets that are NOT `"out"` never burn the account, and a positive hit is undone by a single `POST /consent` with the caller's own shopper token, so the oracle is repeatable at ~2 requests per positive). |
| C9 | **NEW (PR #152 round 3, N4) — the cross-subject consent read (C8) is unaudited.** When subject X's write is refused because subject Y's row said `"out"`, nothing in the immutable log attributes the decision to a second subject's record: `lookupConsent` writes no audit of its own, and `service.ts`'s `remember()` writes NO audit at all for a plain consent-gate refusal (`write.refused` is emitted only for the SEPARATE encryption-fail-closed case — `chat-memory-remember.test.ts` locks this in deliberately). An operator cannot reconstruct that a second subject's consent record drove a given refusal. **Not closed this round** — N2's write-through DOES add a NEW, adjacent audit trail for the specific case where the merge discovers a durable-worthy `"out"` (a `consent.record` entry is written the first time), so an operator inspecting the log can see *that* the account's consent state changed, but that entry does not itself say "because a guest row was consulted" nor identify which guest row. Extending `service.ts`'s own `write.*`/`write.refused` audits to carry a PII-free "a guest row was consulted, merged tiers X/Y" marker (per N4's own text) would need a new `MemoryCtx` field threaded through `remember()` and a change to the pre-existing, tested "consent-denial is silent" invariant (`chat-memory-remember.test.ts`) — a larger, separately-scoped change, not made here to avoid touching that locked-in behavior without its own solution-architect review. | Bounded: the decision itself is still correct and fail-safe (a stranger's row can only make the caller's own write MORE restrictive) — what is missing is reconstructability, not correctness. **Not self-limiting:** an earlier draft said this was self-limiting after N2, inheriting the C8 "one-shot" claim that has since been struck as false. |
| C10 | **NEW (PR #152 round 3) — a third party can DURABLY deny a victim's memory by poisoning their consent.** `/widget/token` mints a tenant token from a *publishable* embed key with no auth, so an attacker holding a victim's guest `anonId` can `POST /consent` recording `"out"` for that subject (this half is pre-existing C1). What round 3's write-through (N2) adds: on the victim's next signed-in `/chat` turn — while their browser still presents that `anonId` — the merge **persists `"out"` onto `acct:<victim>`**. Before N2 this was per-turn and self-healing; it is now durable against new devices, cleared storage, and the victim's own later opt-in. | Direction is privacy-CONSERVATIVE (it can only deny memory, never grant it), which is why it is accepted rather than fixed — but it is a genuine third-party-triggered durable denial and must be signed knowingly. Distinct from C7 (the shopper's own stale record) and C8 (a caller poisoning their own account).  Also note: unlike the other two memory writes on `/chat`, the write-through does NOT carry `!no_autonomous_action`, so a poisoned `"out"` can be durably persisted even on a guardrail-halted turn. Deliberate — the write is strictly restrictive, so suppressing it would only DELAY honoring an opt-out — but recorded here rather than living only in a code comment. The kill switch still overrides it (NN#4, test-locked).  Attribution caveat: a `/consent` call carrying NO shopper token still records `actor: "shopper"` / `source: "shopper"` for a subject the server verified nothing about (this bearer path), so the immutable log cannot distinguish a verified shopper's own choice from a bearer-capability write. |
| C11 | **RESOLVED AS INTENDED — 2026-08-05 (was: consent records survive `/forget`, and one of them is now created autonomously).** `eraseSubject` (`widget-memory/src/erasure.ts`) deletes the subject's vector namespace and audits it; it never touches the `memory_consent` KV. So after a signed-in shopper's "forget everything", both their guest row and the `acct:` row written by N2's merge remain. No surface removes them, and nothing in `docs/` previously recorded this erasure-scope decision. | **DECIDED — the current behaviour is CORRECT and no code change is needed (named-owner decision, 2026-08-05).** The rule, in the owner's words: **"forget" affects PAST data only; it does not mean "don't remember me in future" — opting out is the forward-looking control.** The two are separate instruments and are correctly separate in code: `eraseSubject` clears the fact namespace (backward), the consent record governs what happens next (forward). So retaining an `"out"` is right (it keeps a withdrawal effective — the earlier draft already said so), and retaining an `"in"` is ALSO right, because a shopper who asked to clear their history did not thereby withdraw consent. An earlier revision of this row called retaining an `"in"` "not obviously defensible"; under this decision it is the intended behaviour, not a residual. **Consistency check performed, not assumed:** the widget's own confirmation copy is "Done — I've cleared what I remembered and started fresh", which claims exactly the backward-looking scope and nothing more; and since the manage panel now renders the SERVER's effective state (B11), a shopper whose consent survived erasure sees memory correctly shown as still on rather than the panel implying it was switched off. A shopper who wants both must do both — clear history AND opt out — which is what the panel offers as two separate controls. |
| C12 | **The consent write-through's failure is not in the immutable audit log.** If `recordConsent` throws on the `/chat` path, `server.ts` catches and emits a PII-free console signal (`tenant` + error CLASS only, matching `retention.ts`'s rule) — but NO audit entry, and unlike the retention sweep's signals it omits the hashed `subjectRef` and attempted count, so an operator sees THAT a write-through failed, not for WHICH subject. The turn itself still honors the merged `"out"` (the read-time merge already applied it), so the failure degrades to pre-N2 per-turn behavior, never to a permissive one. (An earlier draft of this row said "no audit entry, no operator signal" and contrasted it with the sweep — both wrong: the console signal exists and uses the same mechanism. Overstating a residual is still misdescribing the code.) | Safe direction, but a persistent store failure would silently mean opt-outs never become durable, identifiable only at the tenant level. |
| C13 | **Two independent shopper-principal derivations.** `/chat` derives the verified shopper inline (`server.ts`) rather than calling `verifiedShopperIdFor`, which `/consent` and `/forget` use. Both apply identical gates today (feature flag, verified merchant principal, `kind === "shopper"` AND the explicit `verified` flag, cross-shop tenant check) and reviewers confirmed equivalence — but it is two implementations of one security decision. | Narrow drift risk, documented in `server.ts` but previously absent from §C, so a signer accepting the residual list never saw it. Unifying them is a follow-up, not a blocker. |

## D. The flip itself (human, in order)

1. Confirm every **A** item is closed and recorded in ADR-0015 — not merely done, but *written down*.
2. Confirm every **B** item is MET and verified against the deployed configuration.
3. Sign off the **C** residuals explicitly, by name.
4. Set ADR-0015 Status → **Accepted**, recording owner + security + legal sign-offs.
5. Flip `MEMORY_ADR_ACCEPTED` to `true` (`packages/widget-memory/src/flag.ts`).
6. Set `MEMORY_ENABLED=true`, `WIDGET_AUTH_REQUIRED=true` **and `SHOPPER_AUTH=true`** in the target
   environment, with the
   encryption key provisioned.
7. **Schedule the retention sweep** (B4) — `pnpm sweep`, as a Cloud Run Job / Kubernetes CronJob, with
   `SHOPIFY_STORES` (and/or `SWEEP_TENANTS`) set so it knows which tenants to visit, and the same
   `DATABASE_URL` + `AUDIT_HMAC_SECRET` as the server. Without this the code is built but never runs, and
   a shopper who never returns is never physically reclaimed — the exact gap B4 exists to close. Verify
   one run's output (`visited`/`deleted`/`retired`/`remaining`) and confirm the `ttl_sweep` entries land
   in the real audit sink (B5). It is safe to schedule BEFORE the flip: with memory off nothing is ever
   written, so the job simply reports zeros.
8. Ship progressively (canary → full) per CLAUDE.md §3.2, watching the audit log and the kill switch.
   **Prod is never auto-deployed.**

**Rollback:** the kill switch halts memory writes immediately at any scope. Reverting the flag const
returns the system to fully inert. Neither undoes data already written — erasure (`/forget`, `erase.subject`)
is the data-removal path, so verify B5/B6 *before* the first real write, not after.
