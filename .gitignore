node_modules/
dist/
.idea/
.env
.env.*
!.env.example
**/secrets/**
*.log
.DS_Store
# generated artifacts
reports/
test-results/
playwright-report/

# self-improvement loop runtime state
.palup-state/

# throwaway one-off scripts (never repo content)
.scratch/

# Claude Code agent worktrees (transient; never repo content)
.claude/worktrees/

# `tsc -b` incremental state — a build artifact, one per referenced project. Newly generated now
# that a root tsconfig exists and `tsc -b` actually runs (it never did before).
*.tsbuildinfo
