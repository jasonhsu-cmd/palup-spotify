# Evidence Ledger — screenshots vs. EVIDENCE.md leads

Method: opened all 7 PNGs in `review-screenshots/` directly (desktop-1440-light.png,
tablet-768-light.png, mobile-390-light.png, mobile-390-viewport.png, panel-390-open.png,
panel-390-card.png, keyboard-focus-skiplink.png). `lighthouse-mobile.json` was NOT opened —
it is JSON data, not a screenshot, so metrics that only live there (CLS, favicon 404) are
flagged below as not visible in any image even though the underlying number/log may be real.
Coordinates are approximate, given in the *displayed* image's own pixel space (top-left
origin), by quadrant.

## Leads 1–10

| # | Issue (from EVIDENCE.md) | Screenshot | What is actually visible | Verified? |
|---|---|---|---|---|
| 1 | Two "Ask the expert" CTAs in different brand colors | `mobile-390-viewport.png`, also `desktop-1440-light.png` | Mobile: terracotta pill button top-left (~x14–207, y302–350, upper-left quadrant) reading "💬Ask the expert"; a separate **evergreen** dark-green floating pill (~x220–332, y778–822, bottom-right corner) also reading "Ask the expert". Two different fills for the same label/action, confirmed. Desktop: terracotta hero CTA (~x112–215, y170–200) vs. evergreen floating launcher (~x762–863, y500–530). | **Yes** |
| 2 | Assistant won't show prices — "current price needs confirming" while storefront shows $50.0 for the same item | `panel-390-card.png` + cross-check `desktop-1440-light.png` | Product card in panel: "The Art of Shaving After Shave Balm - Unscented 3.3 OZ" → grey line reads "current price needs confirming" (~x14–260, y353–367, left-center), no numeric price anywhere on the card. Desktop grid row 1 col 2 shows the identical title with a hard price "$50.0" (~x282–420, y430, upper area). Confirms the hedge vs. storefront price mismatch. | **Yes** |
| 3 | Price format `$35.0` (one decimal) | `desktop-1440-light.png` | Every price tag in the grid uses one decimal place: "$35.0", "$50.0", "$25.0", "$99.0", "$26.0", "$53.0", "$54.0", "$43.0", "$71.0", "$58.0", "$34.0", "$39.0", "$48.0" — visible under every product title across all 6 rows. | **Yes** |
| 4 | Footer policy text truncated mid-word ("…it's basical", "…look 22 agai") | `desktop-1440-light.png` | Bottom cream-colored footer block (~y1476–1575, bottom quadrant). Shipping paragraph ends "...FREE shipping on orders over $50 – treat yourself, it's basical" with no closing punctuation — cut off mid-word ("basical" for presumably "basically"). Returns paragraph ends "...\"It didn't make me look 22 agai" — cut off mid-word ("agai" for "again"), no closing quote. Both confirmed truncated exactly as claimed. | **Yes** |
| 5 | Blank product image tiles (grey/no image) | `desktop-1440-light.png` | Bottom row of the grid, 4 consecutive tiles (~y1300–1490, bottom quadrant, all 4 columns): "CHRISTOPHE ROBIN color fixator wheat germ mask", "Stila Heaven's Hue Highlighter", "CHRISTOPHE ROBIN Regenerating plant oil with rare prickly pear seed oil", "The Art of Shaving Pre-Shave Oil - Sandalwood 2 OZ" — all four render as a flat grey/tan rectangle with zero product photo, while every other tile above has a real image. Also reproduced in `tablet-768-light.png` bottom rows. | **Yes** |
| 6 | Brand FOUC — `<title>`/hero literally renders `{brand}` before JS swaps in "PalUp Skincare Jason" | none | Every screenshot was captured post-load; all show the resolved text "PalUp Skincare Jason" in the header and `<title>`. A FOUC-of-a-template-token is a first-paint timing artifact — a screenshot taken after load structurally cannot show it. | **NOT visible in any screenshot — needs a capture timed at first paint (or a view-source/network-waterfall check)** |
| 7 | Panel cold state has a large empty vertical gap between opener chips and input | `panel-390-open.png` | Greeting bubble + 3 chips ("Find my match" / "Bestsellers" / "New here?") end at ~y250. Then solid white empty space runs all the way to ~y630 (the "Sign in to view your orders" divider) before the input box at ~y755. That's roughly 380px of the ~844px-tall panel — about 45% of the visible panel — completely empty. Confirmed, and worse than a casual read of the lead suggests. | **Yes** |
| 8 | CLS 0.345 (poor layout shift) | none | This is a Lighthouse/CWV numeric metric; nothing in a static screenshot can show a shift-over-time value. `lighthouse-mobile.json` (not opened here, per scope) is the actual evidence artifact for this claim, not a screenshot. | **NOT visible in any screenshot — the claim's real evidence is `lighthouse-mobile.json`, not a PNG** |
| 9 | Favicon 404 | none | Console network error, not a rendered pixel. No screenshot shows browser dev-tools/console output. | **NOT visible in any screenshot — needs a console-panel capture (or the network log referenced in EVIDENCE.md's own "Console: 1 error" line, which is text, not an image)** |
| 10 | Verbose AI reply + verbose footer copy | `panel-390-card.png` (AI reply) + `desktop-1440-light.png` (footer) | Footer: the two paragraphs visible in the desktop footer run to roughly 60–70 words each of jokey copy ("Your glow is on its way. Here's the deal on how it gets to you. 📦Processing time...", "We want your skin and your feelings glowing...") — confirmed verbose/long for a shipping/returns block. AI reply: the visible tail-end bubble in `panel-390-card.png` (~y50–230, top quadrant) is itself a multi-sentence paragraph ("...formula designed to hydrate and calm sensitive skin after shaving. Since the current price for this item needs confirming, I will gladly verify the exact price for you before you make a purchase. Would you like me to check that price, or are you interested in pairing this with an unscented pre-shave oil?") — and the scrollbar thumb sitting near the top of a long track implies additional reply text is scrolled off above. Confirmed verbose, though only the tail of the message is actually visible. | **Yes (footer fully confirmed; AI-reply verbosity confirmed from the visible tail, not the full message)** |

**Score: 7 of 10 leads verified directly from the 7 screenshots. 3 of 10 (FOUC, CLS, favicon 404) are real claims in EVIDENCE.md but are not the kind of thing a static screenshot can show — they need a first-paint capture, the Lighthouse JSON, and a console-panel capture respectively. None of the 3 should be called "false" — they're just unverifiable by image alone.**

## New defects spotted (not in the 10 leads)

| Defect | Screenshot | Location | Description |
|---|---|---|---|
| Duplicate word + inconsistent capitalization in product title | `desktop-1440-light.png` (also reproduced in `mobile-390-light.png`) | Row 1, col 4 (~x610–762, y393–410, upper-right) and row 3, col 4 (~x610–762, y873–890) | Title reads **"The Art Of Shaving Shaving Cream - Bergamot & Neroli 5 OZ"** — capital "Of" (every other product on the page uses lowercase "of", e.g. "The Art of Shaving Pre-Shave Oil"), and the word "Shaving" appears twice back-to-back ("...Shaving Shaving Cream...") because the brand name "The Art of Shaving" collides with the product type "Shaving Cream". This is a data/feed quality bug, not a rendering bug, but it's visibly wrong on the page. |
| Floating launcher occludes product grid content on mobile | `mobile-390-viewport.png` and `mobile-390-light.png` | Bottom-right of `mobile-390-viewport.png` (~x220–332, y778–822); mid-page in `mobile-390-light.png` (~x230–345, y183–228 in the scaled/full-page capture) | The evergreen floating "Ask the expert" launcher sits fixed at the same screen position while the page scrolls, and in both mobile captures it visibly overlaps/covers the top edge of a product tile's photo underneath it (the dark-bottle product in the second grid row). This is a real occlusion of shoppable content by a persistent UI element — not merely stylistic overlap, product image area is physically covered. |

No other misalignment, clipping, disabled-looking controls, or spacing inconsistency was found beyond the above and the 10 listed leads. The skip-link focus box in `keyboard-focus-skiplink.png` (solid black box, "Skip to content", top-left, ~x0–140,y0–40) covers part of the header text underneath it while focused, but that is standard/expected skip-link behavior (it's meant to be visually prominent only on focus) — not logged as a defect.
