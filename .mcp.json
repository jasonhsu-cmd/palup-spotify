{
  "//": "Project-scoped MCP servers, checked in so every dev/agent shares the same tools. None configured — add servers here with env var expansion (${VAR}) for secrets; never commit a secret value. Build-time GitHub work goes through the `gh` CLI, not an MCP server.",
  "mcpServers": {}
}
